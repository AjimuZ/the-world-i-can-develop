# AI Service

This folder is reserved for future AI orchestration code if a separate service becomes necessary.

The MVP should start with a backend-owned mocked LLM client and a replaceable Ollama adapter before introducing any dedicated AI service.
