# Backend

This folder will contain the Spring Boot backend.

The backend will own deterministic game state, validate player actions, control inventory, stats, quests, rewards, combat, Rune effects, and mediate all AI-generated proposals before state changes are applied.

No Spring Boot project has been initialized yet.
